// Letter Home

#include<iostream>
#include<bits/stdc++.h>
using namespace std;

class Solution() {
  public:
    int move() {}
};

int main() {
  int t;
  cin >> t;
  while (t--) {
    int n, s;
    cin >> n >> s;
    vector<int> arr(n);
    for (int i=0; i<n; i++) {
      cin >> arr[i];
    }

    Solution sol;
  }
}