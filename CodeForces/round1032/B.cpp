// B. Above the Clouds

#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

class Solution {
  public:
    void f(int n, string s) {
      // cout << n << " " << s << endl;
    }
};

int main() {
  int t;
  cin >> t;
  while (t--) {
    int n;
    string s;
    cin >> n >> s;

    Solution sol;
    sol.f(n, s);
  }
}